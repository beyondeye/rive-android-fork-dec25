pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}

dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
    }
    versionCatalogs {
        create("fork") {
            from(files("gradle/fork.versions.toml"))
        }
    }
}

rootProject.name = "rive-android"
include ':kotlin'
include ':app'
include ':compatibilitytest'
