package app.rive.mpapp

import androidx.compose.ui.window.ComposeUIViewController

/**
 * iOS entry point.
 * This function creates the UIViewController for iOS.
 */
fun MainViewController() = ComposeUIViewController {
    App()
}
